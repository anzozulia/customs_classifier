// Tailwind v3 pipeline, matching the four official openai-chatkit-advanced-samples apps.
// (v4 would replace both plugins with `@tailwindcss/postcss`; v3 is the version the samples
// and their configs are written against, so copying from them stays a copy.)
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
